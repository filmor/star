from glob import glob

bld(
        features = 'cxx cprogram',
        source = bld.glob("*.cpp"),
        target = 'star',
        uselib = "GLFW BOOST_SYSTEM BOOST_THREAD BOOST_FILESYSTEM LUA FMOD"
    )
